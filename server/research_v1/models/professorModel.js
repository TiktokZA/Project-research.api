// import { db } from "../db/DBcon.js";
import db from "../db/DBcon.js";
class professorMD {
    // async getdata({ proID }) {
    //     console.log(proID);
    //     db.query("SELECT * FROM professor WHERE ID_professor = 24", (error, results, fields)=>{
    //         if (error) throw error;
    //         let massage = "";
    //         if (results === undefined || results.length == 0){
    //             massage = "professor is empty";
    //         }else {
    //             massage = "professor is Succes";
    //         }
    //         return res.send({error: false, data: results, massage: massage});
    //     })
    //     // console.log(Res);
    //     // return Res;
       
    // }
}

export default professorMD;
